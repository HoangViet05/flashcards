import { Navigate, Link } from 'react-router-dom'
import { useAuth } from '../auth/AuthContext'
export default function AccountPage() { const { user, loading, logout } = useAuth(); if (!loading && !user) return <Navigate to="/login" replace />; if (!user) return null; return <main className="account-page"><section className="glass-panel"><p className="eyebrow">Account</p><h1>{user.name || 'Your learning account'}</h1><p>{user.email}</p><div className="account-actions"><Link className="button-secondary" to="/settings">Open settings</Link><button className="button-secondary" onClick={logout}>Sign out</button></div></section></main> }
