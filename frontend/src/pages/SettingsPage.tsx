import { useAuth } from '../auth/AuthContext'
import { useAppearance } from '../providers/AppearanceProvider'
import { useAudio } from '../providers/AudioProvider'
import type { AccentTheme, ThemeMode } from '../types'

export default function SettingsPage() { const { user } = useAuth(); const { theme, accent, reduceEffects, setTheme, setAccent, setReduceEffects } = useAppearance(); const { silent, toggleSilent } = useAudio(); return <div className="settings-page"><header><p className="eyebrow">Your space</p><h1>Settings</h1><p>Preferences are saved to your account when you are signed in.</p></header><section className="glass-panel"><h2>Appearance</h2><label>Theme<select value={theme} onChange={event => setTheme(event.target.value as ThemeMode)}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label><label>Accent<select value={accent} onChange={event => setAccent(event.target.value as AccentTheme)}><option value="violet-cyan">Violet–Cyan</option><option value="blue-emerald">Blue–Emerald</option><option value="amber-rose">Amber–Rose</option><option value="graphite-ice">Graphite–Ice</option></select></label><label><input checked={reduceEffects} onChange={event => setReduceEffects(event.target.checked)} type="checkbox" /> Reduce effects</label></section><section className="glass-panel"><h2>Sound & speech</h2><button className="button-secondary" onClick={toggleSilent}>{silent ? 'Turn Silent mode off' : 'Turn Silent mode on'}</button><p>Music stays off until you choose to play it. Pronunciation never autoplays.</p></section><section className="glass-panel"><h2>Daily goal</h2><p>{user?.preferences?.daily_goal_minutes ?? 15} minutes · {user?.preferences?.timezone ?? 'Asia/Ho_Chi_Minh'}</p></section></div> }
